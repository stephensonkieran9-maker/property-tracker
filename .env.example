# ── Hostaway API ─────────────────────────────────────────────
# Get these from Hostaway: Settings → Hostaway API
# (Account ID is the "client_id", API key is the "client_secret")
HOSTAWAY_ACCOUNT_ID=
HOSTAWAY_API_KEY=

# ── Demo mode ────────────────────────────────────────────────
# Leave the Hostaway vars blank (or set this to "1") to run with
# fake sample data — no API key required.
DEMO_MODE=
